services:
  - type: web
    name: frontend
    env: node
    buildCommand: npm install
    startCommand: npm run dev
    rootDir: Client

  - type: web
    name: backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    rootDir: Server